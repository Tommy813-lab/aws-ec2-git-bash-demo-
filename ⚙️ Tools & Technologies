AWS EC2 (Ubuntu 22.04) — Cloud virtual machine hosting your web server

Git Bash (Windows) — Terminal emulator to run Linux commands and SSH from Windows

WSL (Windows Subsystem for Linux) — Optional Linux environment on Windows for advanced CLI use

Apache2 Web Server — Hosts your static website on the EC2 instance

Bash Scripting — Automates server setup and deployment tasks

Git & GitHub — Version control system and code repository hosting

